@model IEnumerable<StoreFront.DATA.EF.SubType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubTypeID })
        </td>
    </tr>
}

</table>
